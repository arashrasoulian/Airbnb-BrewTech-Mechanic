<div id="top-page-container">
  <%= render "shared/slideshow" %>
  <%= render "shared/filter"  %>
</div>

<div>
  <%= render "shared/home/best_option" %>
</div>

<div >
    <%= render "shared/home/all_flats" %>
</div>


<!--%<div class="row">
  <div class="col-8 row ">

    <% @flats.each do |flat| %>
      <div class="flat col-3 m-3">
        <div class="flat-image">
          <% if flat.photo.attached? %>
            <%= cl_image_tag flat.photo.key, crop: :fill, style: "width: 100px; height: 150px;" %>
          <% else %>
            <%= image_tag "no_image.jpeg", style: "width: 100px; height: 150px;" %>
          <% end %>
        </div>
        <div class="flat-info">
          <h3><%= flat.name %></h3>
          <%= flat.address %>
          <br/>
          <%= flat.city %>
          <br/>
          <%= link_to "Show", flat_path(flat), class: "flat-link" %>
        </div>
      </div>
    <% end %>

  </div>
</div> %-->
